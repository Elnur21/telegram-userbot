const initWhisper = require('./initWhisper')
const displayMessage = require('./displayMessage')
const saveToChannel = require('./saveToChannel')

module.exports = {
  initWhisper,
  displayMessage,
  saveToChannel
}
